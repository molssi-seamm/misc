name: seamm
channels:
  - rdkit
  - default
  - conda-forge
dependencies:
    # Base depends
  - python>=3.7
  - tk=8.6.7
  - pip
  - rdkit
  - openkim-models
  - kim-query

    # other dependencies that are in conda
  - attrs
  - colorama
  - configargparse
  - cycler
  - docutils
  - fasteners
  - jinja2
  - kiwisolver
  - matplotlib
  - openbabel
  - numpy
  - packaging
  - pandas
  - pbr
  - pillow
  - psutil
  - py-cpuinfo
  - pyparsing
  - requests
  - sqlalchemy
  - statsmodels

  # Pip-only installs
  - pip:
        # SEAMM
      - seamm
      - seamm_jobserver
      - seamm_util
      - seamm_widgets
      - seamm_ff_util
      - molsystem
      - reference-handler
      - custom_step
      - forcefield_step
      - from_smiles_step
      - lammps_step
      - loop_step
      - mopac_step
      - packmol_step
      - psi4_step
      - read_structure_step
      - solvate_step
      - supercell_step
      - table_step
