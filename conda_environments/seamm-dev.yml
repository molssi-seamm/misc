name: seamm-dev
channels:
  - rdkit
  - default
dependencies:
    # Base depends
  - python>=3.7
  - tk=8.6.7
  - pip
  - rdkit
  
    # other dependencies that are in conda
  - attrs
  - colorama
  - configargparse
  - cycler
  - docutils
  - fasteners
  - jinja2
  - kiwisolver
  - matplotlib
  - packaging
  - pbr
  - numpy
  - pandas
  - pillow
  - py-cpuinfo
  - pyparsing
  - requests
  - sqlalchemy
  - statsmodels

    # Needed for development
  - coverage
  - flake8
  - pytest
  - pytest-runner
  - sphinx
  - twine

  # Pip-only installs
  - pip:
        # SEAMM
      - seamm
      - seamm_jobserver
      - seamm_util
      - seamm_widgets
      - seamm_ff_util
      - molsystem
      - reference-handler
      - custom_step
      - forcefield_step
      - from_smiles_step
      - lammps_step
      - loop_step
      - mopac_step
      - packmol_step
      - read_structure_step
      - solvate_step
      - supercell_step
      - table_step

        # Needed for development
      - commonmark
      - cookiecutter
      - pytest-datafiles
      - recommonmark
      - sphinx-rtd-theme
      - tox
      - yapf
